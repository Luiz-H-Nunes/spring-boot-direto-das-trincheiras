spring:
  application:
    name: anime-service

server:
  serverlet:
    context-path: /

error:
  include-stacktrace: on-param

threads:
  virtual:
    enable: true

logging:
  level:
    spring: DEBUG